name: CI

on:
  pull_request:
  push:
    branches:
      - "1.21"

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
      - uses: gradle/actions/setup-gradle@v4
      - name: Build
        run: |
          mkdir -p build/ci-reports
          set -o pipefail
          timeout --foreground 1200s ./gradlew clean build --no-daemon 2>&1 | tee build/ci-reports/build.log
      - name: Upload reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-reports
          if-no-files-found: ignore
          path: |
            build/ci-reports/**
            **/build/test-results/**
            **/build/reports/**
